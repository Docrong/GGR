<?xml version="1.0" encoding="GB2312"?>
<!-- SM Proxy 配置文件-->
<config>
  <!--与ISMG(Internet Short Message Gateway)建立连接所需参数-->
  <ismg>

    <!-- ISMG主机地址 -->
    <host>10.110.230.10</host>

    <!-- ISMG主机端口号 -->
    <port>7890</port>

    <!-- 心跳信息发送间隔时间(单位：秒) -->
    <heartbeat-interval>200</heartbeat-interval>

	<!-- 连接中断时重连间隔时间(单位：秒)-->
	<reconnect-interval>200</reconnect-interval>

	<!-- 需要重连时，连续发出心跳而没有接收到响应的个数（单位：个)-->
    <heartbeat-noresponseout>5</heartbeat-noresponseout>

    <!-- 操作超时时间(单位：秒) -->
    <transaction-timeout>30000</transaction-timeout>

	<!--SP…ID(最大为六位字符)-->
	<source-addr>908559</source-addr>

	<!--双方协商的版本号(大于0，小于256)-->
	<version>18</version>

	<!--shared-secret由中国移动与ICP事先商定-->
	<shared-secret>908559</shared-secret>

<!--是否属于调试状态,true表示属于调试状态，所有的消息被打印输出到屏幕，false表示不属于调试状态，所有的消息不被输出-->
  <debug>true</debug>

  </ismg>
</config>
